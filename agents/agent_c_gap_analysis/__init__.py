"""Agent C: candidate-to-job skill gap analysis.

Deterministic by design — zero LLM calls. Imports from ``shared/`` only; the
other agents' internals are out of bounds, per the architecture rule.
"""
