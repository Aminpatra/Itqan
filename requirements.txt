langchain-core>=1.4.9,<2
langchain-openai>=1.0,<2
langgraph>=1.2.9,<2
langgraph-checkpoint>=4.1.1
pydantic>=2.10,<3
paddleocr==3.4.0
paddlepaddle==3.3.0
pymupdf>=1.26
filetype>=1.2
python-dotenv>=1.1
numpy>=2.0
pytest>=8.0

# --- Agent B ---
# httpx is already pulled in transitively by openai, but Agent B depends on it
# directly — declare it, or an openai upgrade could drop it out from under us.
httpx>=0.28
psycopg[binary]>=3.2
pgvector>=0.3
# Parses both the Blogger post bodies (HTML inside Atom <content>) and the
# t.me/s channel pages. The Atom envelope itself uses stdlib ElementTree.
selectolax>=0.3

# Console display ONLY (shared/display.py): terminals like VS Code's (xterm.js)
# cannot shape Arabic, so CLI prints pre-shape it. Never applied to stored data.
arabic-reshaper>=3.0
python-bidi>=0.4

# --- api/ : the web app's backend -----------------------------------------
# FastAPI serves the frontend at /api. Chosen over adding a Node service because
# the pipeline, its contracts (shared/contracts.py) and its read surfaces are all
# Python — a JS backend would have to restate the eligibility predicates and would
# drift from them, which is the class of bug the shared read surfaces exist to
# prevent.
fastapi>=0.115
uvicorn[standard]>=0.34
# Required by FastAPI for multipart Form/File: the document upload and the site's
# login form both post multipart, so without it those routes 500 at import time.
python-multipart>=0.0.20
